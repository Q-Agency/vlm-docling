fastapi==0.115.0
uvicorn[standard]==0.31.0
pydantic==2.9.2
python-multipart==0.0.12

# Docling with VLM support (includes transformers, accelerate, IBM models, etc.)
docling[vlm]==2.58.0

# PyTorch with CUDA 12.6 support (installed separately for specific CUDA version)
torch==2.5.1
torchvision==0.20.1

# Additional dependencies
pillow>=10.0.0
numpy>=1.24.0

