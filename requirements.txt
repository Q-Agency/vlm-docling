fastapi==0.115.0
uvicorn[standard]==0.31.0
pydantic==2.9.2
python-multipart==0.0.12

# Docling with VLM support (includes transformers, accelerate, IBM models, etc.)
# Note: PyTorch is installed separately in Dockerfile for CUDA 12.6 support
docling[vlm]==2.58.0

# Additional dependencies
pillow>=10.0.0
numpy>=1.24.0

